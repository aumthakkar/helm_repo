apiVersion: v1
kind: Service
metadata:
  name: {{ include "testgit.fullname" . }}
  labels:
    {{- include "testgit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testgit.selectorLabels" . | nindent 4 }}
